#!/bin/sh

target=all

while [ $# -ne 0 ]
do
  case "$1" in
    -t | --target) shift;target="$1";;
    *) echo "error: Invalid argument '$1'" 1>&2;exit 1;;
  esac
  shift
done

mkdir -p build && cmake -S . -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build --target "$target"
